{
  "name": "simple-dependency-reexport",
  "version": "1.0.0",
  "license": "MIT",
  "description": "A package the re-exports from simple-dependency",
  "private": true,
  "main": "dist/index.js",
  "sideEffects": [
    "dist/sideEffect.js"
  ],
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.2.3"
  }
}
