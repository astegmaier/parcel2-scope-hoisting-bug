{
  "name": "parcel-repro-simple",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "parcel build src/index.html --no-cache --no-optimize --cache-dir .parcel-cache",
    "clean": "rimraf dist && rimraf .parcel-cache",
    "serve": "http-server dist --port 8081"
  },
  "devDependencies": {
    "parcel": "2.0.0-nightly.632",
    "rimraf": "^3.0.2",
    "http-server": "^0.12.3"
  }
}
