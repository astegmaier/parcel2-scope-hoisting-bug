{
  "private": "true",
  "workspaces": [
    "simple-dependency",
    "simple-dependency-reexport",
    "parcel-repro-simple",
    "parcel-repro-real",
    "webpack-test"
  ],
  "scripts": {
    "clean": "yarn dep clean && yarn simple clean && yarn real clean",
    "build": "yarn dep build && yarn simple build && yarn real build && yarn webpack build",
    "dep": "yarn workspace simple-dependency",
    "simple": "yarn workspace parcel-repro-simple",
    "real": "yarn workspace parcel-repro-real",
    "webpack": "yarn workspace webpack-test"
  }
}
